import { styled, Box } from '@mui/material';

// TokenGraph.s.ts

export const GraphContainer = styled(Box)({
    boxShadow: '0 4px 12px rgba(0, 0, 0, 0.1)',
    height: '35vh',
    borderRadius: '8px',
    overflow: 'hidden',
    border: '1px solid rgba(111, 199, 186, 0.2)',
});

/* Add this CSS rule to your global styles or a CSS file */
// border: '1px solid rgba(111, 199, 186, 0.3)',
